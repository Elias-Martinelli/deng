# Sample data

Small, committed API samples produced by `make explore`
(`scripts/explore_football_api.py`). They document the real source schema and
serve as fixtures for unit tests. Keep every file well below 1 MB; full raw data
lives in `data/raw/` (git-ignored) locally and in Google Cloud Storage in the
final architecture.
